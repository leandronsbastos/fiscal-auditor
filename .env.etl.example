# Configuração do ETL de NF-e/NFC-e

# Diretório padrão para monitoramento/processamento
# Quando você executa 'python run_etl.py' sem argumentos, este diretório será usado
DIRETORIO_PADRAO=

# Deletar arquivos após processamento bem-sucedido
# true = Remove os XMLs após serem processados e salvos no banco
# false = Mantém os arquivos originais no diretório
DELETAR_APOS_PROCESSAR=true

# Mover para diretório de backup (alternativa ao deletar)
# true = Move arquivos processados para o diretório de backup
# false = Não move arquivos (serão deletados se DELETAR_APOS_PROCESSAR=true)
# NOTA: Se DELETAR_APOS_PROCESSAR=true, esta opção é ignorada
MOVER_PARA_BACKUP=false

# Diretório de backup (usado se MOVER_PARA_BACKUP=true)
DIRETORIO_BACKUP=

# Validação de duplicatas por chave de acesso
# true = Verifica se NF-e já existe no banco pela chave de acesso
# false = Não valida (pode causar erros de integridade)
VALIDAR_POR_CHAVE=true

# Validação de duplicatas por hash do arquivo
# true = Calcula hash SHA256 e verifica se arquivo já foi processado
# false = Não valida por hash (permite reprocessar mesmo XML)
# ÚTIL: Detecta se o mesmo XML foi baixado novamente com nome diferente
VALIDAR_POR_HASH=true
